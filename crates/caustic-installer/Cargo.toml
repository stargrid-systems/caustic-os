[package]
name = "caustic-installer"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
caustic-installer-core = { path = "../caustic-installer-core" }
iced = { version = "0.14", features = ["tokio", "sipper"] }
tokio = { workspace = true, features = ["sync", "macros"] }
